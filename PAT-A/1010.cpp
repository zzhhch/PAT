#include<cstdio>
#include<cstring>
#include<algorithm>
using namespace std;

long long trans(long long a,int rad)
{
	long long ans=0;
	while(a)
	{
	}
}

int main()
{
	long long a,b,tag,radix;
	scanf("%lld%lld%lld%lld",&a,&b,&tag,&radix);
	if(tag==2)
	{
		swap(a,b);
	}

}